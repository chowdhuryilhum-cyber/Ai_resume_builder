import { useState } from 'react';
export function Auth({ mode, onSuccess, onBack }) {
  const [form, setForm] = useState({ name: '', email: '', password: '' }); const [error, setError] = useState(''); const [busy, setBusy] = useState(false);
  const register = mode === 'register';
  async function submit(e) { e.preventDefault(); setBusy(true); setError(''); try { const response = await fetch(`/api/auth/${register ? 'register' : 'login'}`, { method: 'POST', credentials: 'include', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(form) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.message); onSuccess(payload.user); } catch (err) { setError(err.message || 'Unable to sign in.'); } finally { setBusy(false); } }
  return <main className="auth-page"><div className="auth-card"><button className="back link" onClick={onBack}>← Back to home</button><a className="brand" href="#top">✦ ResumeCraft <span>AI</span></a><h1>{register ? 'Create your account' : 'Welcome back'}</h1><p>{register ? 'Save resumes securely and access them whenever you need.' : 'Log in to manage your saved resumes.'}</p><form onSubmit={submit}>{register && <label>Full name<input required value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} placeholder="Ilhum ul Hoque"/></label>}<label>Email<input type="email" required value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} placeholder="you@example.com"/></label><label>Password<input type="password" minLength="8" required value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} placeholder="At least 8 characters"/></label>{error && <p className="auth-error">{error}</p>}<button className="primary auth-submit" disabled={busy}>{busy ? 'Please wait…' : register ? 'Create account' : 'Log in'}</button></form><p className="switch">{register ? 'Already have an account?' : 'New to ResumeCraft?'} <button className="link" onClick={() => onBack(register ? 'login' : 'register')}>{register ? 'Log in' : 'Create an account'}</button></p></div></main>;
}
